import React from "react";
import "./Flat.css";

const Flat = (props) => {
  /**
   * Check if flat is the current selected flat
   */
  // const isSelected =

  /* Return statement */
  return <div>Single flat</div>;
};

export default Flat;
